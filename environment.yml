# conda env create --file environment.yml
name: cookiecutter_testing
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.10
  - pip:
    - pyhere
    